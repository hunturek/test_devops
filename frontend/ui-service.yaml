apiVersion: v1
kind: Service
metadata:
  name: titanic-ui
spec:
  type: NodePort
  selector:
    app: titanic-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000